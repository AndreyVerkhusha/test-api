# Задание 1.

## Запуск проекта
```bash
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate
php artisan serve
```

## API - получить список jokes из бд
[http://127.0.0.1:8000/api/records](http://127.0.0.1:8000/api/records)

## Ручной запуск команды (для автоматического запуска нужен cron)
```bash
php artisan jokes:fetch
```

## Почему добавлен репозиторий
В этой задаче репозиторий не был обязательным, потому что логика получения данных очень простая.  
Я добавил его, чтобы показать,что понимаю подход с разделением ответственности и работу с данными вынес в отдельный слой.

# Задание 2.
Для второй части задания добавил скрипт в файл filter-fields-by-type.js <br>
Его можно скопировать, вставить в консоль в панели разработчика на https://test.amopoint-dev.ru/testzz/testlist.html </br>
Затем переключать значения в поле "Тип".

На выполнение тестового затрачено времени около 1-ого часа

